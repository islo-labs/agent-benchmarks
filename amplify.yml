version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --prefix software-factory
    build:
      commands:
        - npm run build --prefix software-factory
        - mkdir -p _site/software-factory
        - cp software-factory/dist/. _site/software-factory/
        - cp index.html landscape.html privacy.html favicon.svg _site/
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'
